更新部分：
更新了一堆sh，都在本文件夹
eval逻辑重构，目前eval选的仍然是train里面的50个，但貌似无影响
因为测试要进行多次train
eval目前对于多次train的多个文件夹里面每个文件夹的所有pth只会选择最后一次的pth的最后一个last.pth
目前eval就只测这一次并且应该可以正常退出
train仍然是每5轮一个pth没有变
train.py关于eval的代码已经修改，源代码命名为vlnce2
死锁轮数已改，主要在collect阶段
collect阶段保存rgb与depth已删
collect测试了batchsize，gpu
train测试了batchsize，epoch
eval测试了batchsize
collect运行会删掉之前的collect，务必保证最后一次collect是自己信得过的，保证可以用！！！